{
	"private": true,
	"type": "module",
	"scripts": {
		"start": "eleventy --serve --quiet",
		"build": "rm -rf dist && eleventy && npm run mp3",
		"mp3": "./scripts/mp3.sh",
		"deploy": "rsync --copy-links --archive --verbose --compress --delete --delete-excluded --exclude '.DS_Store' ./dist/ deploy@3lp.me:/opt/content/",
		"test": "editorconfig-checker",
		"new": "cp -r template src/episodes/$npm_config_episode"
	},
	"engines": {
		"node": ">=24"
	},
	"browserslist": [
		"baseline widely available, safari 15"
	],
	"devDependencies": {
		"@11ty/eleventy": "^3.1.2",
		"@babel/core": "^7.29.0",
		"@babel/preset-env": "^7.29.0",
		"del": "^8.0.1",
		"editorconfig-checker": "^6.1.1",
		"esbuild": "^0.27.2",
		"html-minifier-terser": "^7.2.0",
		"js-yaml": "^4.1.1",
		"lightningcss": "^1.31.1",
		"pretty-data": "^0.40.0"
	}
}
