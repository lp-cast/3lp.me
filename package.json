{
	"private": true,
	"type": "module",
	"scripts": {
		"start": "eleventy --serve --quiet",
		"build": "rm -rf dist && eleventy",
		"deploy": "rsync --archive --progress --compress --delete --delete-excluded --exclude '.DS_Store' ./dist/ deploy@3lp.me:/opt/content/",
		"test": "editorconfig-checker",
		"new": "cp -r template src/episodes/$npm_config_episode"
	},
	"engines": {
		"node": ">=24"
	},
	"browserslist": [
		"baseline widely available, safari 15"
	],
	"devDependencies": {
		"@11ty/eleventy": "^3.1.2",
		"@babel/core": "^7.28.5",
		"@babel/preset-env": "^7.28.5",
		"del": "^8.0.1",
		"editorconfig-checker": "^6.1.1",
		"esbuild": "^0.27.1",
		"html-minifier-terser": "^7.2.0",
		"js-yaml": "^4.1.1",
		"lightningcss": "^1.30.2",
		"pretty-data": "^0.40.0"
	}
}
